// const blockstack = require('blockstack');

/*
export const isSignedIn = () => {
  return window.blockstack.isUserSignedIn();
};

export const signIn = (redirUrl, manifestUrl, scope) => {
  window.blockstack.redirectToSignIn(redirUrl, manifestUrl, scope);
};

export const signOut = () => {

};*/